"""{{ description }}"""

# Data format: parquet, csv, arrow, jsonl
FORMAT = "parquet"

# Column schema (optional, for documentation)
SCHEMA = {
    # "column_name": "type",
}

# Set automatically by `dreadnode push`
ARTIFACTS_HASH = None